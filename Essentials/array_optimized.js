/**
* this simple show two loops optimized
*/

// first example loop 
for (var i = Things.length - 1; i >= 0; i--) {
	Things[i]
}

//best example
var Things = [], i = Things.length;
while(i--){
	Things[i];
}